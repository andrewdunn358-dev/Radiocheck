{
  "buildCommand": "npx expo export -p web",
  "outputDirectory": "dist",
  "framework": null,
  "installCommand": "yarn install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
